{
  "_comment": "Inventory of this fork's divergence from frappe/wiki. Regenerate with: python3 scripts/neoffice-divergence.py --update. A dropped entry is a Neoffice change that an upstream merge ate.",
  "marked_blocks": 233,
  "files": {
    ".github/workflows/build-frontend.yml": [
      "wiki-app.html, not wiki.html: wiki v3 renamed the"
    ],
    ".github/workflows/fork-markers.yml": [
      "` markers: a hunk that changes"
    ],
    ".github/workflows/neoffice-divergence.yml": [
      "added file (no upstream equivalent)."
    ],
    ".gitignore": [
      "commit-the-build: upstream ignores every generated asset",
      "trailing newline only. Upstream ends this file without one;"
    ],
    "CLAUDE.md": [
      "everything down to \"Notes amont\" is ours (unshipped file,",
      "rewritten. Upstream tells the reader to ignore the fork and"
    ],
    "NEOFFICE_FORK_MARKERS.md": [
      "` comment",
      "` marker block in the"
    ],
    "frontend/index.html": [
      "brand. Upstream ships \"Frappe Wiki\"; this title is what a"
    ],
    "frontend/src/App.vue": [
      "computed + useRoute added for the viewKey below (header space switcher).",
      "added. SpaceDetails reads props.spaceId once, at setup: its space",
      ":key added, see viewKey in the script above: without it a"
    ],
    "frontend/src/components/DiffViewer.vue": [
      "useStorage('wiki-theme') import dropped, useTheme() imported",
      "was useStorage('wiki-theme', 'dark'); now the shared ref (see"
    ],
    "frontend/src/components/NeoCockpitBridge.vue": [
      "added file (no upstream equivalent). Mounts the shared"
    ],
    "frontend/src/components/NeoCockpitWikiSidebar.vue": [
      "added file (no upstream equivalent). Wiki flavour of the"
    ],
    "frontend/src/components/Sidebar.vue": [
      "\"Wiki\", not \"Frappe Wiki\": we ship this under the",
      "added. Anonymous visitors and signed-in users without a wiki",
      "nothing to log out of when browsing anonymously.",
      "change requests need a wiki role; readers only get the reading"
    ],
    "frontend/src/components/SpaceList.vue": [
      "added. Readers are anonymous visitors AND signed-in users",
      "readers only ever see published spaces, so the Status column",
      "the Status column is spread in only for editors (see the note",
      "closes the computed() wrapper; upstream ends a plain array here.",
      "wrapped in computed() because `columns` is now reactive (see",
      "readers can't reach `frappe.client.get_list` on Wiki Space",
      "auto:false, loaded by the watcher below. It used to read",
      "split in two: the editor's list resource is built here and the",
      "auto:false; the watcher below loads it once the user store",
      "reactive, and it has to be. This read `const spaces =",
      "and the matching side: whichever resource `spaces` now points at",
      "reader list is unpaginated",
      ".value: `spaces` is a computed now (see above).",
      ".value: `spaces` is a computed now (see above). Only an"
    ],
    "frontend/src/components/SpaceTreePanel.vue": [
      "the space name is a switcher, the way the public",
      "added. The theme toggle used to sit in this header and",
      "gated on canManageTabs (the space's can_write). Upstream",
      "the v-if on the next line is ours; the reason is in the",
      "Dropdown, computed and useRouter added for the space switcher",
      "added: the theme toggle we put back in this header needs it.",
      "props captured (upstream calls defineProps without binding it):",
      "added. Spaces to offer in the header switcher. The parent",
      "everything below is ours: the space switcher and the theme"
    ],
    "frontend/src/components/WikiDocumentPanel.vue": [
      "added button. Server-side PDF of this article",
      "v-if added. The menu holds authoring actions only (rename,",
      "createResource added: the reader branch below fetches through a",
      "added. Anonymous visitors and signed-in users without a wiki",
      "readers can't read Wiki Document through frappe.client.get",
      "added. Public PDF export of this article, rendered server-side"
    ],
    "frontend/src/components/tiptap-extensions/ImageNodeView.vue": [
      "two changes inside this img tag, both listed in",
      "added. Upstream has no way to resize an image;",
      "added. Readers need to enlarge screenshots; the",
      "computed added: imageStyle and currentSize below (image sizing",
      "everything from here to setCustomWidth() is ours: image size",
      "styles for the added size controls */",
      "lightbox styles. Global (not scoped) on purpose: the markup"
    ],
    "frontend/src/components/tiptap-extensions/MermaidBlockView.vue": [
      "useStorage dropped from this import and useTheme added on the",
      "was useStorage('wiki-theme', 'dark') read directly here, which",
      "the shared ref, per the note above."
    ],
    "frontend/src/composables/useTheme.js": [
      "rewritten wholesale. Upstream stores useStorage('wiki-theme',",
      "added, part of the rewrite described at the top of this file:",
      "kept inside our own applyTheme, which also sets the shared",
      "the rest of our rewrite (block above systemTheme): apply the",
      "added: upstream compared userTheme.value inline in each caller.",
      "reads isDark instead of comparing the raw ref (same icon).",
      "toggling now WRITES the choice (upstream only mutated the",
      "applyTheme also sets the ref now, so upstream's trailing",
      "added. Drops the explicit choice and hands the theme back to",
      "applies the module ref (upstream applied the storage ref).",
      "return widened: upstream returns userTheme/themeIcon/"
    ],
    "frontend/src/layouts/MainLayout.vue": [
      "added. Shared Neoffice chrome (ADR-015); it falls",
      "was `import Sidebar from '../components/Sidebar.vue'`. The",
      "added. Readers are anonymous visitors AND signed-in users"
    ],
    "frontend/src/main.js": [
      "translationsReady added to this import; it is what the mount",
      "mount AFTER the translations land. __() is not reactive: any"
    ],
    "frontend/src/pages/SpaceDetails.vue": [
      "readers have nothing to contribute from: no draft,",
      "two props of this component are ours and are listed in",
      "same two props as the desktop panel above (:readonly",
      ":readonly below was `isGitSynced`: a reader must get the",
      "added. Readers are anonymous visitors AND signed-in users",
      "added branch. Readers can't read Wiki Space through",
      "added. Readers go through upstream's very own read-only path:",
      "the capabilities endpoint needs a session; a reader can",
      "added. Feeds the space switcher in the tree panel's header (we",
      "`url` below: a reader gets a 403 from get_wiki_tree (v3 gates",
      "get_public_space_info wraps the same payload as {space, tree}.",
      "was `if (isGitSynced.value)`: readers read the tree through the",
      "readers take the same read-only path: no CR to hydrate from.",
      "same widening as in treeData: a reader's refresh must re-run"
    ],
    "frontend/src/router.js": [
      "added route. Pretty URL routing: our wikis are linked as",
      "rewritten guard. Upstream bounces EVERY route to /login when",
      "signed in but with no wiki role (portal Website User):",
      "added. Readers land directly in the first public space rather",
      "single terminal next(). Upstream ended on an if/else that only"
    ],
    "frontend/src/stores/user.js": [
      "added. Holds one of the wiki authoring roles. Everyone else —",
      "rewritten. Upstream gated the app shell on holding an",
      "was Boolean(is_logged_in). Change requests need an authoring",
      "exported: components branch on the authoring role, not on"
    ],
    "frontend/src/translation.js": [
      "added. translate() reads window.translatedMessages at render",
      "returns the resource (upstream discarded it) so"
    ],
    "pyproject.toml": [
      "was \">=3.14\" upstream (v3 targets a Python the fleet does not"
    ],
    "scripts/neoffice-divergence.py": [
      "added file (no upstream equivalent).",
      "` at the time — the marking"
    ],
    "wiki/api/__init__.py": [
      "allow_guest added (upstream: @frappe.whitelist()). The SPA asks",
      "everything from here to resolve_wiki_path() is ours; upstream",
      "ignore_permissions is safe ONLY because can_read_space()",
      "this used to return every published space to anyone. It fed",
      "added. The two resolvers below are ours and both allow_guest.",
      "added. Both resolvers used to climb `parent_wiki_document` in a",
      "this resolver is ours (pretty URLs) and it is allow_guest. It",
      "was `if name:`. Any row matching the route was handed",
      "this raw-SQL fallback bypassed every check above, so a slug",
      "was `if row: return {\"space_id\": row[0][\"name\"]}`. Filtered",
      "ours too, and allow_guest like the resolver above. Same hole,",
      "bounded climb; see climb_to_root_document() above.",
      "was an unconditional return. It disclosed the",
      "same filter as resolve_space_slug: a space the caller",
      "filtered too, and un-LIMIT-ed for the same reason as in",
      "same as in resolve_space_slug: filtered, and looped instead of"
    ],
    "wiki/api/og_image.py": [
      "guard the frappe.utils.preview import (upstream imports it bare).",
      "added: lets callers ask whether OG cards can be rendered at all",
      "bail out explicitly when the renderer is missing (v15), so a"
    ],
    "wiki/api/wiki_space.py": [
      "allow_guest added (upstream: @frappe.whitelist()). Neoffice wikis",
      "the tree is filtered for the caller. check_permission(\"read\")",
      "include_drafts added (upstream always returns everything). See",
      "was `filters={\"name\": (\"in\", documents)}`. The caller may",
      "added (no upstream equivalent). Readers without a wiki role —",
      "enforce the space's own access rules. Filtering on",
      "added (no upstream equivalent). Same reason as",
      "is_published is always 1 given the filter above, but",
      "a published page still belongs to a space; check that space",
      "bounded climb (was a bare `while True`): a cycle in",
      "fail CLOSED. This used to read `if space_id and not"
    ],
    "wiki/frappe_wiki/doctype/wiki_change_request/test_wiki_change_request.py": [
      "the indexing queue is v16-only; on v15 the merge indexes"
    ],
    "wiki/frappe_wiki/doctype/wiki_document/search.py": [
      "this paragraph used to end \"Orphan documents (no wiki_space)",
      "is_published and is_private come along now. The space was the",
      "`is_private` is the LEGACY per-page guest flag: wiki v3 removed it",
      "is_published and is_private added; see above.",
      "added, memoised like the one above. Drafts and private pages",
      "orphan hits (no wiki_space) used to pass unconditionally, so",
      "a hit with no Wiki Document row is a stale index entry for",
      "orphan hits (no wiki_space) used to pass unconditionally",
      "and the page's own state, not just its space's. Editors keep"
    ],
    "wiki/frappe_wiki/doctype/wiki_document/test_wiki_document.py": [
      "this fork makes a space public through one honest checkbox,",
      "this fork adds a master switch, Wiki Settings.enable_public_wiki",
      "restore the master switch, see setUpClass.",
      "added test (no upstream equivalent). Upstream's switcher query",
      "added test (no upstream equivalent). Upstream offers the Edit",
      "generated OG cards need a renderer Frappe v15 does not ship",
      "our process_navbar_items drops a row without a URL instead of",
      "same as test_empty_url_has_no_icon: the row is dropped."
    ],
    "wiki/frappe_wiki/doctype/wiki_document/wiki_document.py": [
      "skip items with no URL. Upstream renders every row, and the",
      "added (upstream inlined a bare frappe.get_all in",
      "\" (is_published=0) to anonymous visitors — the content 404'd, but",
      "docstring reworded (88b1f47). Upstream's text promised the",
      "is_wiki_author added to this import; it is what the guard",
      "Edit is for authors only. Upstream shows it to everyone and",
      "the `if not space: return` that stood here is gone. It let",
      "routed through _spaces_for_switcher() (see that",
      "og_images_supported added to this import (upstream imports",
      "no renderer on Frappe v15 (see wiki/api/og_image.py), so",
      "pdf_generator added (upstream lets Frappe pick, which"
    ],
    "wiki/frappe_wiki/doctype/wiki_document/wiki_sqlite_search.py": [
      "added: this app's index stays idempotent HERE, not in frappe.",
      "SQLiteSearch.add_to_queue (and the scheduler drain) exist in",
      "see the block marker above: v15 queue fallback"
    ],
    "wiki/frappe_wiki/doctype/wiki_revision_item/patches/add_revision_doc_key_index.py": [
      "added file (no upstream equivalent)."
    ],
    "wiki/frappe_wiki/patches/init_public_read_from_guest_role.py": [
      "added file (no upstream equivalent)."
    ],
    "wiki/frappe_wiki/patches/redirect_bare_wiki_route_to_app.py": [
      "added file (no upstream equivalent)."
    ],
    "wiki/hooks.py": [
      "note, not code. v3 moved the editor SPA from /wiki to /wiki-app,"
    ],
    "wiki/patches.txt": [
      "patches below are ours; keep them last so an upstream merge"
    ],
    "wiki/permissions.py": [
      "added (no upstream equivalent). The line between the two",
      "added. Master switch for anonymous access, off by default.",
      "added. THE definition of \"this caller has no account and the",
      "anonymous visitors get nothing while the master switch is",
      "the master switch was missing here while can_read_space()",
      "allow_null was hardcoded True, so every list query got the",
      "was `return True`, which handed every orphan document to"
    ],
    "wiki/templates/wiki/document.html": [
      "user-facing strings wrapped in _() so they land in the POT",
      "neo-wiki-article added. Hook for our reader CSS, which",
      "i18n: the same two hardcoded English labels as in macros/buttons.html, which was fixed while this copy was missed. This is the hide_chrome branch (orphan documents), so it renders on a page the space layout never reaches. tojson plus forceescape, not bare quotes, so an apostrophe in a translation cannot break the Alpine expression. #}",
      "i18n: upstream hardcodes this label in English; _() puts it in the POT so the public reader speaks the client's language. #}",
      "i18n: upstream passes these menu labels to the macro as raw English; _() puts them in the POT. #}",
      "i18n: upstream passes these menu labels to the macro as raw English; _() puts them in the POT. #}",
      "i18n: same dropdown as above, desktop copy; upstream labels are raw English. #}",
      "i18n: same dropdown as above, desktop copy; upstream labels are raw English. #}",
      "i18n: upstream concatenated the English words with the date; _() with a placeholder lets a translator move the date. #}"
    ],
    "wiki/templates/wiki/includes/feedback_widget.html": [
      "user-facing strings wrapped in _() so they land in the",
      "i18n: upstream hardcodes this question in English; _() puts it in the POT. #}",
      "i18n: the aria-label of the fieldset below is read aloud by screen readers, so it is translated too. #}",
      "i18n on the placeholder attribute of this textarea: upstream",
      "i18n: upstream hardcodes the submit label in English. #}",
      "i18n: upstream hardcodes the thank-you in English. #}"
    ],
    "wiki/templates/wiki/includes/header.html": [
      "user-facing strings wrapped in _() so they land in the POT",
      "i18n on the title attribute of this button: upstream hardcodes",
      "i18n on the aria-label below: upstream hardcodes it in English. #}",
      "i18n: upstream hardcodes the search placeholder in English. #}",
      "i18n on the aria-label below: upstream hardcodes it in English. #}"
    ],
    "wiki/templates/wiki/includes/mobile_header.html": [
      "user-facing strings wrapped in _() so they land in the POT",
      "i18n: upstream hardcodes this label in English. #}",
      "i18n on the aria-label of this button: upstream hardcodes it in",
      "i18n on the title attribute below: upstream hardcodes it in English. #}",
      "i18n on the aria-label below: upstream hardcodes it in English. #}"
    ],
    "wiki/templates/wiki/includes/sidebar.html": [
      "was the English literal 'Last updated '; translated here so"
    ],
    "wiki/templates/wiki/includes/toc.html": [
      "user-facing strings wrapped in _() so they land in the POT",
      "i18n: upstream hardcodes this heading in English. #}"
    ],
    "wiki/templates/wiki/layout.html": [
      "brand. Upstream falls back to \"Frappe Wiki\"; this reader is",
      "brand, see the note above: the marker had to be repeated here",
      "added. Reader styling of ours (wider articles, image size",
      "nginx `deny all`s every /vendor/ path (403 on every instance, breaking Alpine-driven sidebar/header/search/feedback); moved wiki/public/js/vendor/ to third_party/ and updated these paths (ee770cd \"fix(reader): nginx denies every /vendor/ path — move the vendored assets\") #}",
      "see the block marker above: third_party/ rename #}"
    ],
    "wiki/templates/wiki/macros/buttons.html": [
      "user-facing strings wrapped in _() so they land in the POT",
      "i18n: _() applied to the macro argument (upstream printed",
      "i18n on the aria-label below: upstream hardcodes it in English. #}",
      "i18n: both labels were hardcoded English next to an already-translated line; tojson plus forceescape, not bare quotes, so an apostrophe in a translation cannot break the Alpine expression. #}",
      "i18n: upstream hardcodes the PDF download description in English. #}"
    ],
    "wiki/test_permissions.py": [
      "public_read is the source of truth for \"this space is on the",
      "enable_public_wiki is the master switch for the whole",
      "restore the master switch (see setUp).",
      "added. The master switch is the one thing standing between a"
    ],
    "wiki/utils.py": [
      "early return added (WI-00297, ab732fd). Upstream gives the"
    ],
    "wiki/wiki/doctype/wiki_page/sqlite_search.py": [
      "TO REVIEW at the next merge: this retry is NOT ours. It comes",
      "call renamed with the function, see the note in search() above:",
      "renamed from _search() to free that name for the retry wrapper"
    ],
    "wiki/wiki/doctype/wiki_space/patches/v3/assign_wiki_user_to_active_users.py": [
      "restrict to desk users. Upstream granted \"Wiki User\" to EVERY"
    ],
    "wiki/wiki/doctype/wiki_space/wiki_space.js": [
      "added handlers (no upstream equivalent). The Wiki Sidebars grid"
    ],
    "wiki/wiki/doctype/wiki_space/wiki_space.py": [
      "added. \"Public\" upstream means a Guest row buried in the"
    ],
    "wiki/www/wiki-app.html": [
      "brand. Upstream ships \"Frappe Wiki\"; this title is what a"
    ],
    "wiki/www/wiki_app.py": [
      "added: the SPA shell is served to visitors without an account",
      "no anonymous access while the wiki is private: send them to",
      "a reader never enters the authoring app. /wiki-app is ours;",
      "upstream calls frappe.local.response_headers.set() directly.",
      "added (no upstream equivalent). Translates an authoring-app URL"
    ]
  }
}
